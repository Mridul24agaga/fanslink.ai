// app/layout.tsx
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'TikFusion+',
  description: 'Generated by create next app',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-[#000319] text-white antialiased">{children}</body>
    </html>
  )
}

// components/gradient-bg.tsx
export default function GradientBG() {
  return (
    <div aria-hidden="true">
      {/* very subtle vignette and soft light, no bright tints */}
      <div className="pointer-events-none absolute inset-0 -z-10">
        {/* soft vignette */}
        <div className="absolute inset-0 bg-[radial-gradient(60%_40%_at_50%_0%,rgba(255,255,255,0.08),rgba(0,0,0,0))]" />
        {/* faint mesh lights */}
        <div className="absolute left-[-10%] top-[-10%] h-64 w-64 rounded-full bg-white/5 blur-[80px]" />
        <div className="absolute right-[-10%] top-[10%] h-64 w-64 rounded-full bg-white/4 blur-[80px]" />
        <div className="absolute bottom-[-10%] left-[30%] h-72 w-72 rounded-full bg-white/3 blur-[100px]" />
        {/* ultra subtle grid */}
        <div className="absolute inset-0 bg-[linear-gradient(to_right,rgba(255,255,255,0.04)_1px,transparent_1px),linear-gradient(to_bottom,rgba(255,255,255,0.04)_1px,transparent_1px)] bg-[size:56px_56px] opacity-20" />
      </div>
    </div>
  )
}

// components/site-header.tsx
"use client"

import type React from "react"
import Link from "next/link"
import { Menu } from 'lucide-react'
import { Button } from "@/components/ui/button"
import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet"

export default function SiteHeader() {
  return (
    <header className="sticky top-0 z-40 w-full border-b border-white/10 bg-[#000319]/85 backdrop-blur">
      <div className="container mx-auto flex h-16 items-center justify-between px-4">
        <Link href="/" className="flex items-center gap-2 font-semibold">
          <span className="inline-flex h-7 w-7 items-center justify-center rounded-md bg-white text-black">TF</span>
          <span className="text-lg text-white">TikFusion+</span>
        </Link>

        <nav className="hidden items-center gap-6 md:flex">
          <HeaderLink href="#">Products</HeaderLink>
          <HeaderLink href="#">Affiliate</HeaderLink>
          <HeaderLink href="#">FAQ</HeaderLink>
          <HeaderLink href="#">Telegram</HeaderLink>
        </nav>

        <div className="hidden md:block">
          <Button className="rounded-full bg-white text-black hover:bg-zinc-200">Account</Button>
        </div>

        <div className="md:hidden">
          <Sheet>
            <SheetTrigger asChild>
              <Button variant="outline" size="icon" className="h-9 w-9 border-white/20 bg-transparent text-white">
                <Menu className="h-4 w-4" />
              </Button>
            </SheetTrigger>
            <SheetContent side="right" className="w-72 bg-[#000319] text-white">
              <div className="mt-8 grid gap-4">
                <MobileLink href="#">Products</MobileLink>
                <MobileLink href="#">Affiliate</MobileLink>
                <MobileLink href="#">FAQ</MobileLink>
                <MobileLink href="#">Telegram</MobileLink>
                <Button className="mt-2 bg-white text-black hover:bg-zinc-200">Account</Button>
              </div>
            </SheetContent>
          </Sheet>
        </div>
      </div>
    </header>
  )
}

function HeaderLink({ href, children }: { href: string; children: React.ReactNode }) {
  return (
    <Link href={href} className="text-sm text-zinc-300 transition-colors hover:text-white">
      {children}
    </Link>
  )
}

function MobileLink({ href, children }: { href: string; children: React.ReactNode }) {
  return (
    <Link
      href={href}
      className="rounded-md px-2 py-2 text-base text-zinc-200 transition-colors hover:bg-white/10 hover:text-white"
    >
      {children}
    </Link>
  )
}

// app/page.tsx
import { ArrowRight, Quote } from 'lucide-react'
import Image from "next/image"
import Link from "next/link"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader } from "@/components/ui/card"
import { Separator } from "@/components/ui/separator"
import SiteHeader from "@/components/site-header"
import GradientBG from "@/components/gradient-bg"

const testimonials = [
  {
    name: "DylanOFM",
    title: "OFM Agency Owner",
    quote:
      "If you get 1 video that goes viral, with TikFusion, you have 100 more lottery tickets to go viral again.",
  },
  { name: "Derrek Brown", title: "CEO of Untouched Agency", quote: "The tool is very good. I use it across 50,000 IG pages." },
  {
    name: "Luca",
    title: "OFM Agency Owner",
    quote: "I tested every other spoofer. This is the only one that makes my content unique for TikTok & IG.",
  },
]

export default function Page() {
  return (
    <main className="relative min-h-screen overflow-hidden bg-[#000319] text-white">
      <GradientBG />
      <SiteHeader />

      <section className="relative">
        <div className="container mx-auto px-4 pt-20 md:pt-28">
          <div className="mx-auto max-w-4xl text-center">
            <p className="inline-flex items-center rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-zinc-400 backdrop-blur">
              New • Improved workflow and brand‑safe exports
            </p>

            <h1 className="mt-6 text-balance text-4xl font-extrabold tracking-tight sm:text-5xl md:text-6xl">
              <span className="text-white">Make </span>
              <span className="bg-gradient-to-r from-white via-indigo-300 via-violet-400 to-amber-200 bg-clip-text text-transparent">
                {"1000s of Videos"}
              </span>
              <span className="text-white"> from 1 </span>
              <span className="bg-gradient-to-r from-white via-indigo-300 via-purple-400 to-amber-200 bg-clip-text text-transparent">
                {"and Upload"}
              </span>
            </h1>

            <p className="mx-auto mt-6 max-w-2xl text-pretty text-lg text-zinc-400">
              Repurpose once, distribute everywhere. Create a system that grows revenue faster by publishing to all
              platforms with one click.
            </p>

            <div className="mt-8 flex flex-col items-center justify-center gap-3 sm:flex-row">
              <Button size="lg" className="group rounded-full bg-white px-6 py-6 text-base text-black hover:bg-zinc-200">
                Get TikFusion
                <ArrowRight className="ml-2 h-4 w-4 transition-transform group-hover:translate-x-0.5" />
              </Button>
              <Button
                size="lg"
                variant="outline"
                className="group rounded-full border-white/20 bg-transparent px-6 py-6 text-base text-white hover:bg-white/10"
              >
                Ultimate TikTok & IG Guide
                <ArrowRight className="ml-2 h-4 w-4 transition-transform group-hover:translate-x-0.5" />
              </Button>
            </div>

            <div className="mt-10 grid grid-cols-1 gap-4 sm:grid-cols-3">
              {testimonials.map((t, i) => (
                <Card key={i} className="border-white/10 bg-white/5 backdrop-blur">
                  <CardHeader className="flex flex-row items-center gap-3 pb-2">
                    <div className="flex h-10 w-10 items-center justify-center rounded-full bg-white/10">
                      <Quote className="h-5 w-5 text-white" />
                    </div>
                    <div>
                      <div className="font-semibold leading-tight text-white">{t.name}</div>
                      <div className="text-xs text-zinc-400">{t.title}</div>
                    </div>
                  </CardHeader>
                  <CardContent className="pt-0 text-sm text-zinc-300">{t.quote}</CardContent>
                </Card>
              ))}
            </div>

            <div className="mx-auto mt-10 max-w-4xl">
              <Separator className="bg-white/10" />
              <div className="mt-6 grid grid-cols-2 place-items-center gap-6 opacity-60 sm:grid-cols-4">
                <Image src="/trusted-logo-1.png" alt="Trusted logo 1" width={120} height={40} />
                <Image src="/trusted-logo-v2.png" alt="Trusted logo 2" width={120} height={40} />
                <Image src="/trusted-logo-3.png" alt="Trusted logo 3" width={120} height={40} />
                <Image src="/trusted-logo-4.png" alt="Trusted logo 4" width={120} height={40} />
              </div>
            </div>
          </div>
        </div>
      </section>

      <footer className="relative mt-16 border-t border-white/10 bg-[#000319]/80 py-10 backdrop-blur">
        <div className="container mx-auto flex flex-col items-center justify-between gap-4 px-4 sm:flex-row">
          <p className="text-sm text-zinc-400">© {new Date().getFullYear()} TikFusion+. All rights reserved.</p>
          <div className="flex items-center gap-6 text-sm text-zinc-400">
            <Link href="#" className="hover:text-white">Privacy</Link>
            <Link href="#" className="hover:text-white">Terms</Link>
            <ReferenceDialog />
          </div>
        </div>
      </footer>
    </main>
  )
}

function ReferenceDialog() {
  return (
    <details className="cursor-pointer">
      <summary className="list-none underline underline-offset-4 hover:text-white/90">View reference design</summary>
      <div className="mt-4 overflow-hidden rounded-lg border border-white/10 bg-[#000319]">
        <Image
          src="/images/reference.png"
          alt="Reference landing page screenshot used for this redesign"
          width={1400}
          height={800}
          className="h-auto w-full"
        />
      </div>
    </details>
    )
  }
